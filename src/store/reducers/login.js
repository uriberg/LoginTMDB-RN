
const initialState = {
  loginMode: false,
  isLoggedIn: false,
};


const login = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default login;
